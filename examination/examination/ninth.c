/*------------------------------------------------------
【程序改错】
--------------------------------------------------------

功能：输入两个实数，按代数值由小到大输出它们,并在fun()
      函数中输出。（输出的数据都保留2位小数）

------------------------------------------------------*/

#include <stdio.h>

void fun()
{
    float t
        float a, b;
    scanf("%f %f", &a, &b);
    if (a < b)
    {
        t = a;
        a = b;
        b = t;
    }
    printf("%5.2f ,%5.2f\n", &a, &b);
}

int main()
{
    fun();
    return 0;
}