/*------------------------------------------------------
【程序改错】
--------------------------------------------------------

功能：编写函数fun求20以内所有5的倍数之积。

------------------------------------------------------*/

#define N 20
#include <stdio.h>

int fun(int m)
{
    int s = 0, i;
    for (i = 1; i < N; i++)
        if (i% m = 0)
            s = *i;
    return s;
}

int main()
{
    int sum;
    sum = fun(5);
    printf("%d以内所有%d的倍数之积为： %d\n", N, 5, sum);
    return 0;
}