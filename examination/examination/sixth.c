/*-------------------------------------------------------
【程序填空】
---------------------------------------------------------

题目： 以下程序按每行5个数的格式输出1至n范围内能被7或17 整除的整数，并求出其和。
       如输入：50  则输出：
       7       14      17      21      28
       34      35      42      49
       Sum=247


-------------------------------------------------------*/
#include <stdio.h>
int main()
{
    int  i, n, ct = 0, sum = 0;
    scanf("%d", &n);
    for (i = 1; i <= n; i++)
        /***********SPACE***********/
        if (【 ? 】)
        {
            printf("%d\t", i);
            /***********SPACE***********/
            【 ? 】;
            ct++;
            /***********SPACE***********/
            if (【 ? 】)
                printf("\n");
        }
    printf("\nSum=%d\n", sum);
    return 0;
}