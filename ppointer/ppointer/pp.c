#include<stdio.h>

int main()
{
	//char *p2
	//不管是什么类型的指针，都是创建指针变量
	//指针变量是用来存放地址的
	//指针变量的大小取决于一个地址存放的时候需要大多空间
	//32位的机器上的地址：32bit位——4byte，所以指针变量的大小事4个字节
	//64位机器上的地址：63bit位——8byte，所以指针变量的大小是8个字节

	int 魑[10] = { 10 * 0 };

	return 0;
}